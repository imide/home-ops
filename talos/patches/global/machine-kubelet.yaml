machine:
  kubelet:
    extraArgs:
      - destination: /var/openebs/local
        type: bind
        source: /var/openebs/local
        options:
          - rbind
          - rshared
          - rw

    extraConfig:
      serializeImagePulls: false
    nodeIP:
      validSubnets:
        - 10.0.0.87/24
